import { createStore } from "vuex";
import Auth from "./Auth.js";
export default createStore({
  state: {},
  getters: {},
  mutations: {},
  actions: {},
  modules: {
    namespace: true,
    Auth,
  },
});
